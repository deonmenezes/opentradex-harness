{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'curl -s --max-time 2 http://127.0.0.1:3210/api/ >/dev/null 2>&1 && echo \"[opentradex] gateway up on 3210\" || echo \"[opentradex] gateway not running — start with: npx opentradex run\"'"
          }
        ]
      }
    ]
  }
}
